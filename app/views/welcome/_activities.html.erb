
<% @activities = PublicActivity::Activity.all.order('updated_at') %>
<% @activities.each do |a|%>


	<!-- tasks -->
	<% if a.key.start_with?('task') %>
	<%= changed_task = Task.find(a.trackable_id) %>
		<% if a.key.ends_with?('create') %>
			<%= 'Task erstellt' %>
		<% end %>
		<br> 
	<% end %>
	<!-- tasks\ -->

<% end %> 
